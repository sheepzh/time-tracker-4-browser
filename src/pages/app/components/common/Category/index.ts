import Editable from './Editable'
import Select from './Select'

const Category = {
    Select: Select,
    Editable: Editable,
}

export default Category
export { type Instance as CategorySelectInstance } from './Select'
